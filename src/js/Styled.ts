import styled from "styled-components"

export const HeaderTitle = styled.div`
  position: relative;
  h3 {
    font-size: 18px;
    color: #253778;
    font-weight: 700;
    text-align: center;
    margin: 0;
  }
  .action {
    position: absolute;
    right: 0;
    top: 0;
  }
`
